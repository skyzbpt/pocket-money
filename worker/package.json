{
  "name": "guoding",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node cors-test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
